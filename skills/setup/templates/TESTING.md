# 테스트

> 이 문서는 **질문에 답하는 식**으로 채워요. 빈 헤딩만 남겨두면 아무도 안 읽어요.
> 답이 없는 항목은 "없음 — 이유" 라고 적어요. 비워두는 것과 없다고 적는 건 달라요.

## 어떤 명령으로 돌리나

<!-- 복붙해서 바로 실행되는 명령을 적어요. 전제(설치·env)가 있으면 함께. -->

```bash
# 예) npm test / pytest / go test ./... / cargo test
```

- 전체 실행:
- 하나만 실행:
- 감시 모드(있으면):

## 무엇을 테스트하고, 무엇을 안 하나

<!-- "다 테스트한다"는 답이 아니에요. 안 하는 것을 적는 게 이 절의 목적이에요. -->

| 대상 | 테스트하나 | 왜 |
| --- | --- | --- |
|  |  |  |

## 새 코드에 테스트를 어디까지 붙이나

<!-- 팀 기준. "커버리지 N%" 든 "버그 수정엔 재현 테스트 필수" 든 판단 가능한 문장으로. -->

## 검증을 어떻게 남기나

<!-- 테스트가 없는 변경(설정·문서·인프라)은 무엇으로 "확인했다"고 하나?
     스크린샷·로그·임시 환경 실측 등, 다음 사람이 재현할 수 있는 형태로. -->

## 이 프로젝트에서 자주 깨지는 것

<!-- 실제로 겪은 것만. 아직 없으면 "아직 없음" 이라고 적고, 겪으면 여기에 추가해요. -->
