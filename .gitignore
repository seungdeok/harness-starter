# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
out/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Cache
.cache/
.vite/

# oh-my-claudecode runtime/generated state — 전부 무시해요 (ADR-011).
# 슬래시 앞에 경로가 없어 모든 깊이의 .omc 를 잡아요 (하위 .omc 는 잘못된 cwd 로 실행된 흔적).
.omc/
.claude/.cc-writes/

# Claude Code local (개인) 설정 — 플러그인 활성화 등, 커밋하지 않아요
.claude/settings.local.json

# phase 별 git worktree (pipeline.py init 이 생성) — 레포 안이지만 추적 제외
.claude/worktrees/
